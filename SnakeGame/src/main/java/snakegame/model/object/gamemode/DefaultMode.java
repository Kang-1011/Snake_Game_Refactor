package snakegame.model.object.gamemode;

import snakegame.model.GameManager;

/**
 * Class representing the default game mode and its operation
 */
public class DefaultMode implements Level {
    /**
     * Operation in default mode
     * @param gameManager game manager
     */
    @Override
    public void executeLevelFeatures(GameManager gameManager) {

    }
}
